// const ErrorHandler = (err, req, res, next) => {
// 	console.log('Middleware Handling Errors');
// 	const errStatus = err.statusCode || 500;
// 	const errMsg = err.msg || 'Something went wrong';
// };
